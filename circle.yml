machine:
  node:
    version: 6.9.4

dependencies:
  cache_directories:
    - ~/.cache/yarn
  pre:
    - curl -o- -L https://yarnpkg.com/install.sh | bash
    - yarn --version
  override:
    - yarn

test:
  override:
    - yarn test
    - yarn run build
